# ollama_call.py
def scan_image(image_path):
    # 假设 ollama 库已经导入并配置
    # 在这里添加 ollama 扫描图片的代码，并返回扫描结果
    # 示例：result = ollama.scan(image_path)
    # 此处仅为示例，需替换为实际的 ollama 调用代码
    result = f"Scanned {image_path}"  # 示例返回值
    return result
